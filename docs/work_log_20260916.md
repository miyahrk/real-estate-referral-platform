# 作業ログ 2026-09-16

**対象：** 不動産紹介プラットフォームMVP 開発環境構築〜初期整備
**関連：** `real_estate_referral_platform_kickoff_instructions_20260916.md`

---

## 1. 開発環境の構築

着手指示書ステップ0（開発環境構築）を実施。

- **Node.js v24.19.0（LTS）**を winget でインストール
- **Eclipse Temurin JDK 21.0.12.1（LTS）**を winget でインストール（`JAVA_HOME`をユーザー環境変数に設定。管理者権限がなくMachine全体には設定不可だったため）
- **PostgreSQL 16** はネイティブインストールせず、既存のDocker Desktop上でコンテナ運用する方針に変更（バージョン管理・アンインストールの手間を避けるため）

## 2. プロジェクトの初期化

- **フロントエンド**：`create-next-app`でNext.js（App Router, TypeScript, Tailwind CSS, src構成）を初期化
- **バックエンド**：Spring Initializr経由でSpring Bootプロジェクトを生成
  - 依頼時点でのSpring Boot最新安定版が4.1.1だったため採用（指示書作成時点の想定バージョンから変更）
  - groupId: `com.miyazaki.realestate` / artifactId: `referral-platform` / Java 21
  - 依存関係: Web, Data JPA, PostgreSQL Driver, Validation, Lombok, Security
- **docker-compose.yml**でPostgreSQLコンテナを定義
- Node.js/JDK/Docker/Next.js/Spring Bootそれぞれ起動確認（DB接続含む）を実施し、正常動作を確認

## 3. ドキュメント作成

- `docs/dev_environment.md` — インストール済みツール・バージョン・パス・起動手順を記録
- `docs/architecture.md` — モジュール構成・依存関係・仕組みを記録

## 4. プロジェクト名の見直し

- ルートフォルダ名を `platform/` から **`real-estate-referral-platform/`** に変更
  - 理由：`platform`単体では汎用的すぎて、他プロジェクトが増えた際に紛らわしいため。Spring Bootの`artifactId`（`referral-platform`）とも一致させる形で決定
- リネーム作業中、OneDriveの同期ロックにより `mv`/`Move-Item` が断続的に失敗し、`.git`が2箇所に分裂する事故が発生
  - 対応：作業ファイルの内容に欠損がないことを確認した上で、壊れた`.git`を破棄し`git init`からやり直し（コミット履歴はリセットされたが、ファイル内容の損失なし）

## 5. DBパスワードの環境変数化

- `docker-compose.yml`・`application.properties`に直書きしていたPostgreSQL接続情報を、Git管理外のファイルに分離
  - Docker Compose用：`.env`（実値） / `.env.example`（テンプレート、コミット対象）
  - Spring Boot用：`backend/src/main/resources/application-local.properties`（実値） / `application-local.properties.example`（テンプレート）
- `.gitignore`に上記の実値ファイルを追加
- 再起動して動作確認済み（Docker/Spring Boot双方とも新方式で接続成功）
- 併せて、`docs/`配下のドキュメント自体もGit管理対象のため、ドキュメント内に実パスワードを記載しない方針に修正

## 6. Gitリポジトリの整理

- 企画書2件（着手指示書・MVPスコープ定義書）を `Real_Estate_Business/` 直下から `real-estate-referral-platform/docs/` に移動し、プロジェクト関連ドキュメントを一元化
- 現時点のコミット履歴：
  ```
  c923749 Externalize DB credentials out of version control
  0ee8094 Scaffold real estate referral platform MVP dev environment
  ```
- リモート（GitLab等）は**未接続**。個人ネームスペースにprivateプロジェクトを作成し接続する方針で合意

## 7. 未対応・持ち越し事項

- GitLabへのプッシュ（プロジェクトURL待ち）
- 着手指示書ステップ1（DBスキーマ・マイグレーションファイル作成）以降は未着手

---

## 更新履歴

- 2026-09-16：初版作成
